mutation($userId: String!, $tokenName: String!, $siteKey: String, $expireAt: String, $tokenState: TokenState) {
    admin {
        personalApiTokens {
            createToken(userId: $userId, name: $tokenName, site: $siteKey, expireAt: $expireAt, state: $tokenState)
        }
    }
}
